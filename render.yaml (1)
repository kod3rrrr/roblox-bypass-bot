services:
  - type: web
    name: roblox-bypass-bot
    env: python
    buildCommand: pip install -r requirements.txt && playwright install firefox && playwright install-deps firefox
    startCommand: python main.py
    envVars:
      - key: PLAYWRIGHT_BROWSERS_PATH
        value: /opt/render/.cache/ms-playwright